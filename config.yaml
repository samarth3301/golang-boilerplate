server:
  port: "8080"
  host: "0.0.0.0"

database:
  url: "postgres://postgres:postgres@localhost:5432/golang-boilerplate?sslmode=disable"

redis:
  url: "localhost:6379"

jwt:
  secret: "your-super-secret-jwt-key-change-this-in-production"